name: LintLang

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read
  security-events: write

jobs:
  agent-instructions:
    uses: hermes-labs-ai/.github/.github/workflows/reusable-lintlang.yml@main
    with:
      path: AGENTS.md
